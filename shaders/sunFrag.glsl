precision mediump float;
precision mediump int;

varying vec3 vPos;

void main() {
	vec3 outColor = vec3(244.0 / 255.0, 233.0 / 255.0, 155.0 / 255.0); 
	gl_FragColor = vec4(outColor, 1.0);
}

	//252, 177, 3


